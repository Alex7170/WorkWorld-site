<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>WorkWorld</title>
</head>
<body>
    {{#if isAuthorized}}
        {{> headerTrue}}
    {{else}}
        {{> headerFalse}}
    {{/if}}
    <div>
        <h2>Requests</h2>
        {{#each users}}
            <div>
                <h3>Looking for {{this.title}}</h3>
                <p>Experience: {{this.experince}}</p>
                <p>{{this.other}}...  {{this.salary}}</p>
            </div>
        {{/each}}
    </div>
</body>
</html>